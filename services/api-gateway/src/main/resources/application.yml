server:
  port: ${PORT:8080}

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      httpclient:
        connect-timeout: 3000
        response-timeout: 10s

      globalcors:
        add-to-simple-url-handler-mapping: true
        cors-configurations:
          '[/**]':
            allowedOrigins:
              - ${CORS_ALLOWED_ORIGINS:http://localhost:5173}
            allowedMethods:
              - GET
              - POST
              - PUT
              - PATCH
              - DELETE
              - OPTIONS
            allowedHeaders:
              - "*"
            allowCredentials: true

      default-filters:
        # Downstream may also send CORS headers; keep a single value after merge.
        # (CorsWebFilter sets ACAO before the chain — do NOT RemoveResponseHeader those.)
        - DedupeResponseHeader=Access-Control-Allow-Origin Access-Control-Allow-Credentials, RETAIN_FIRST
        - AddRequestHeader=X-Gateway-Secret, ${GATEWAY_SHARED_SECRET:local-gateway-secret}

      routes:
        - id: auth-service
          uri: ${AUTH_SERVICE_URL:http://localhost:8081}
          predicates:
            - Path=/api/v1/users,/api/v1/users/**

        - id: story-service
          uri: ${STORY_SERVICE_URL:http://localhost:8082}
          predicates:
            - Path=/api/v1/stories,/api/v1/stories/**

        - id: sync-service
          uri: ${SYNC_SERVICE_URL:http://localhost:8083}
          predicates:
            - Path=/api/v1/reading-progress,/api/v1/reading-progress/**